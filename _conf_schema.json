{
  "bot_self_id": {
    "type": "string",
    "default": "",
    "description": "机器人QQ号（必填）"
  },
  "debug": {
    "type": "bool",
    "default": false,
    "description": "开启调试日志"
  },
  "max_inject": {
    "type": "int",
    "default": 50,
    "description": "每次注入的最大历史记录条数（仅检索注入）"
  },
  "max_search_limit": {
    "type": "int",
    "default": 500,
    "description": "单次检索数据库最多返回的记录条数（LIMIT）"
  },
  "custom_stopwords": {
    "type": "text",
    "default": "",
    "description": "自定义停用词，每行一个词，避免误过滤核心术语"
  },
  "use_jieba": {
    "type": "bool",
    "default": true,
    "description": "是否使用 jieba 分词（提高关键词准确性），若未安装 jieba 则自动安装并启用"
  }
}
